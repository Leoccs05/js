//const nomes = new Array('Edu', 'Felipe', 'Beatriz');
/*const nomes = ['Edu', 'Felipe', 'Beatriz'];
const novo = [...nomes]; //isso se torna uma copia, diferente de const novo = nomes
console.log(nomes.length)
nomes[2] = 'Marcio';
console.log(nomes)
delete novo[2]
const removido = nomes.pop()//shift para eliminar o primeiro
nomes.push('joao')
nomes.unshift('Pedro')
nomes.unshift('Cris')
console.log(nomes, removido)

const agora = nomes.slice(0, -2)
console.log(agora)*/

const nome = 'Luiz Otavio Miranda'
const nomes = nome.split(' ')
console.log(nomes)
const inver = [ 'Luiz', 'Otavio', 'Miranda' ]
const revni = inver.join(' ')
console.log(revni)